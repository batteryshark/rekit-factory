---
id: T-049
title: "[rekit] E2 · Harness adapter + subagent fan-out"
status: done
assignee: null
priority: high
tags:
  - rekit
depends_on:
  - T-048
created_at: 2026-07-05
updated_at: 2026-07-06
---

**Target repo:** rekit

## Context

- Epic **E2**. Plan: `parallax/docs/rekit-epic.md` §E2. Depends on E1 (T-048).
- Replaces the pydantic-ai + MiniMax decider with a harness-agnostic adapter. First harness = **pi**.
- This is the **cut line for pydantic-ai** (E2.4).

## Goal

Define `HarnessAdapter` (`invoke(brain, prompt, tools, ledger_context) → actions`) + the ralph loop
driving it over the ledger; ship one real adapter (pi); add subagent fan-out; delete pydantic-ai.

## Acceptance Criteria

- [ ] The loop runs a goal to termination via the adapter interface — no pydantic-ai in the path.
- [ ] The **pi** adapter drives an end-to-end goal (e.g. "unpack + analyze") on a real brain.
- [ ] Subagent fan-out: two independent trees analyze concurrently, converging on one ledger;
      fanned-out subagents inherit the **scoped** skill set (pi via pi-subagents).
- [ ] pydantic-ai + the MiniMax decider are **deleted**; no core dependency remains.
- [ ] Model-tier routing: the adapter takes a per-invocation tier hint — routine/floor steps hit
      **MiniMax M3** (cheap), heavy-judgment steps hit **z.ai GLM 5.2** (beefy). Both are wired into
      pi on this machine. The deterministic floor uses no model.

## Notes

- Keep the adapter honest by sketching a second (claude) adapter after pi lands.
- Tiers M3/GLM 5.2 are the two providers pi is configured with locally — see the loop's step-weight
  tagging; tier is a loop decision, not a per-skill constant.

## Progress
