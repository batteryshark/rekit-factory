---
id: T-047
title: "[rekit] E0 · Scaffold the rekit runtime repo"
status: done
assignee: null
priority: high
tags:
  - rekit
depends_on: []
created_at: 2026-07-05
updated_at: 2026-07-06
---

**Target repo:** rekit

## Context

- Epic **E0** of the rekit/parallax pivot. Plan of record: `parallax/docs/rekit-epic.md`.
- Topology decision: `rekit` is a **new** repo (the orchestration runtime), depends on **nothing**.
- Unblocks E1–E4, E6, E7.

## Goal

Scaffold the `rekit/` repo skeleton — the home for the orchestration runtime (ralph loop, ledger,
skill loading, human channel, dashboard).

## Acceptance Criteria

- [ ] `rekit/` exists as its own git repo at the workspace root.
- [ ] Minimal package layout + uv env; the package imports; a test runner is wired and green.
- [ ] README states scope: runtime only; depends on nothing (not even parallax).
- [ ] `.gitignore` / CI basics in place, consistent with the other repos.

## Notes

- No parallax dependency. This is pure kernel scaffolding — no orchestration logic yet (that's E1+).

## Progress

### Implementation (rekit/) — DONE, commit `7f367e6`

- New independent git repo at `runner-lab/rekit/` (own `main`, sibling of parallax/parallax-taxonomy).
- `pyproject.toml`: package `rekit`, `requires-python >=3.11`, setuptools, **`dependencies = []`**,
  console entry `rekit = rekit.cli:main`.
- Docstring-only stub subpackages: `ledger/` (E1), `loop/` (E2), `harness/` (E2, with the
  `invoke(brain, prompt, tools, ledger_context, tier) -> actions` seam noted), `skills/` (E3),
  `human/` (E4); plus `cli.py` (argparse `main()`, `--version`).
- `tests/test_smoke.py` (plain-python + pytest-compatible): version, subpackage imports, cli.main.
- `README.md` states scope (runtime; depends on nothing incl. parallax); `.gitignore` (python+uv).
- Verified: uv venv (CPython 3.12.11), `uv pip install -e .` → `rekit==0.1.0`; smoke test green;
  `rekit --version` → `rekit 0.1.0`; no accidental parallax imports (only doc-path references).

### Review (orchestrator)

- Verified independently: git commit present, 7 module files, smoke test green, `dependencies = []`,
  no stray parallax/prlx imports. All 4 acceptance criteria met. → `done`.
